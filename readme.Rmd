---
output:
  html_document:
    df_print: kable
    code_folding: "hide"
    theme: united
    highlight: zenburn
---

# Today's selection
```{r}
library("csv")
asana <- read.csv("asana.csv")

selection <- 12
total <- nrow(asana)

# Take sample of all asana
s <- sample(total, selection)
asana[s,]
```

# All asana
```{r}
asana
```