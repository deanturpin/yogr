---
title: "YOGR"
date: "Created with &hearts; by a computer on `r format(Sys.time())`"
output:
  html_document:
    df_print: kable
    code_folding: "hide"
    theme: united
    highlight: zenburn
---

```{r, yogr}
library("csv")
asana <- read.csv("asana.csv")

# Select a few
selection <- 12
total <- nrow(asana)

# Take sample of all asana
s <- sample(total, selection)
asana[s,1:3]
```
