---
title: "YOGR"
date: "Created with &hearts; by a Google Cloud instance on `r format(Sys.time())`"
output:
  html_document:
    df_print: kable
    code_folding: "hide"
    theme: united
    highlight: zenburn
---

```{r}
library("csv")
asana <- read.csv("asana.csv")

# Take a sample of all asana
s <- sample(nrow(asana), 12)
asana[sort(s),1:3]
```
